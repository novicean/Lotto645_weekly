{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/lotto645.py", // 어떤 python 파일을 실행할 것인지
            "args": ["--buy_lotto"], // argument 집어넣어서 실행할 경우
            "console": "integratedTerminal"
        }
    ]
}